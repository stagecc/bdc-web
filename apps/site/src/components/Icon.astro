---
import SharedIcon from '@bdc/ui-astro/icon/Icon.astro';

interface Props {
  name: string;
  label?: string;
  size?: 3 | 4 | 5 | 6 | 7 | 8 | 9;
  class?: string;
}

const props = Astro.props as Props;
---

<SharedIcon {...props} />
