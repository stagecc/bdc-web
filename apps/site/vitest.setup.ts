/*
 * This setup file runs before every test file.
 */

// Register custom matchers like `toBeInTheDocument()`, `toHaveValue()`, etc.
// so they don't need to be imported in every test file.
import '@testing-library/jest-dom/vitest';
